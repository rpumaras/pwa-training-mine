var path = require('path');
var webpack = require('webpack');

module.exports = {
    context: __dirname + '/src',
    entry: './index',
    output: {
        path: path.resolve(__dirname, './'),
        filename: 'index.js',
        libraryTarget: 'umd'
    }
};
