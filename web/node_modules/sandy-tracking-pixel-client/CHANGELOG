v0.1.18
    - Allow Cross domain tracking via the `mobify_id` query string
v0.1.17
    - Add a `title` attribute to the iframe that `_trackAjax()` created,
      in order to meet certain WCAG guideline
v0.1.16
v0.1.15
    - Extend the expiration on the client_id cookie to 2 years
    - Set the slug for a tracker on the first create, don't allow further create calls to change it
    - Bug fix for using 'event' api before initialization
v0.1.14
    - Improve our iframe handling and restore non-blocking request
v0.1.13
    - HOTFIX: Fixed issue with iframe and no body object
v0.1.12
    - HOTFIX: Fixed issue with Hijax and XHR loading
v0.1.11
    - Fix pixel blocking `load` event
v0.1.10
    - Allow multiple tracker init
v0.1.9
    - Allow eventChannel to be specified in event calls
v0.1.8
    - Tracker init has separate logic when `window.document` does not exist.
      This allows Service Workers to record analytics
v0.1.7
    - Node style completion callback as optional parameter to pageview, timing,
      and event methods
    - Uses fetch api to fire tracking pixel when `fetch()` is available
v0.1.6
    - New analytics endpoint: https://engagement-collector.mobify.net/s.gif
v0.1.5
    - Now properly compatible with the provided snippet
v0.1.4
    - Remove exception throwing codepaths
v0.1.3
    - Prioritize browserify style loading ahead of amd style loading to avoid
      accidental calls to define when built into a browserify bundle
v0.1.2
    - 'create' comamnd now idempotent or mutating for default tracker and named
      trackers, rather than destructive
    - Sandy.init() now tries to detect previous calls and bails on subsequent
      ones
v0.1.1
    - package.json and bower.json updated to package name
      'sandy-tracking-pixel-client'
v0.1.0
    - Initial tagged release
